# discord-utility-suite

A Discord bot with slash commands for moderation, tickets, polls, and reminders.

## Stack

Primary language: **TypeScript**

## Project structure

- `src/index.ts` — Community management and utility commands for Discord servers.
- `package.json` — dependencies

## Usage

Review the source files and install any dependencies referenced by the project before running it.
